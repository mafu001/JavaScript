//project of number

//console.log(process.argv); // here are 2 array 0= node.js binary patch and 1=javascript file path

let k = parseInt(process.argv[2]);
let m = parseInt(process.argv[3]);

console.log(k + m); // to get a result open terminal and write node file-name number number(then enter)
